// Roles Enum
export const enum Role {
  Teacher = "teacher",
  Student = "student",
  Parent = "parent",
  Admin = "admin",
}

// Languages Enum
export const enum Languages {
  English = "en",
  Arabic = "ar",
}

// Direction Enum
export const enum Directions {
  LTR = "ltr",
  RTL = "rtl",
}
